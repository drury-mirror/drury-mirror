{
  "name": "drury-mirror",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "database": "docker compose up",
    "database:setup": "npm --prefix ./backend run database:setup",
    "database:edit": "npm --prefix ./backend run database:edit",
    "backend": "npm --prefix ./backend run dev"
  },
  "dependencies": {
    "drury-mirror-backend": "file:./backend"
  }
}
